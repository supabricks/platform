# Reference material — external clones and build outputs, not our source
/postgres/
/pgroot/
/reference/

# Local key material — regenerate per spike/compose/README.md ("Generating the JWKS")
spike/compose/compute_wrapper/private-key.pem
spike/compose/compute_wrapper/public-key.pem
spike/compose/compute_wrapper/public-key.der

# OS / editor noise
.DS_Store
*.log

# Pageserver runtime data (bind-mounted live cell state, written by the running pageserver)
spike/compose/pageserver_config/tenants/
