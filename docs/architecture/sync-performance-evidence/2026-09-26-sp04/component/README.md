# Matched planning component evidence

24 measurements, 12 pairs, three pairs per age/key cell. All plans match and all pairs pass with no detected build/test overlap. The frozen driver constructs fixtures before quiet admission. Raw results, host observations, private-profiler fixed-label streams, and fixture file hashes are retained; synthetic Parquet files can be regenerated by the driver. Metrics count Python filesystem APIs, not all native metadata operations.
